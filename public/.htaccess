# Risk Pro Consulting - SPA Routing & Optimization
# This file is copied to the build output (dist) by Vite since it's in /public.
# Place the contents of dist/ into public_html on cPanel; keep this .htaccess.

# ---------------------------------
# 1. Force HTTPS (uncomment if SSL is active)
# ---------------------------------
# <IfModule mod_rewrite.c>
#   RewriteEngine On
#   RewriteCond %{HTTPS} !=on
#   RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
# </IfModule>

# ---------------------------------
# 2. Single Page Application (SPA) Routing
# ---------------------------------
<IfModule mod_rewrite.c>
RewriteEngine On
RewriteBase /
# Allow direct access to existing files and directories
RewriteCond % { REQUEST_FILENAME } -f [OR]
RewriteCond % { REQUEST_FILENAME } -d
RewriteRule ^ - [L]
# Redirect everything else to index.html
RewriteRule . /index.html [L]
</IfModule>

# ---------------------------------
# 3. MIME Types (ensure correct serving)
# ---------------------------------
<IfModule mod_mime.c>
AddType application/javascript js mjs
AddType text/css css
AddType image/svg+xml svg
AddType application/manifest+json webmanifest
</IfModule>

# ---------------------------------
# 4. Compression (Gzip / Deflate)
# ---------------------------------
<IfModule mod_deflate.c>
AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css application/javascript application/json application/xml image/svg+xml
# Exclude already compressed content
SetEnvIfNoCase Request_URI \.(?:gif | jpe?g | png | webp | avif)$ no-gzip dont-vary
</IfModule>

# ---------------------------------
# 5. Caching Headers
# ---------------------------------
<IfModule mod_expires.c>
ExpiresActive On
# Static assets (hashed by Vite) can be cached longer
ExpiresByType application/javascript "access plus 7 days"
ExpiresByType text/css "access plus 7 days"
ExpiresByType image/png "access plus 30 days"
ExpiresByType image/jpeg "access plus 30 days"
ExpiresByType image/webp "access plus 30 days"
ExpiresByType image/avif "access plus 30 days"
ExpiresDefault "access plus 1 hour"
</IfModule>

# ---------------------------------
# 6. Security Headers (basic; adjust as needed)
# ---------------------------------
<IfModule mod_headers.c>
Header set X-Content-Type-Options "nosniff"
Header set X-Frame-Options "SAMEORIGIN"
Header set Referrer-Policy "strict-origin-when-cross-origin"
Header set Permissions-Policy "camera=(), microphone=(), geolocation=()"
# Uncomment after verifying no inline script issues:
# Header set Content-Security-Policy "default-src 'self'; img-src 'self' data: https:; style-src 'self' 'unsafe-inline'; script-src 'self'; font-src 'self' data:; connect-src 'self';"
</IfModule>

# ---------------------------------
# 7. Disable Directory Listing
# ---------------------------------
Options -Indexes

# ---------------------------------
# 8. Custom 404 (optional - SPA fallback usually catches it)
# ---------------------------------
# ErrorDocument 404 /index.html

# End of .htaccess
